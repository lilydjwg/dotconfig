[options]
overlay = true
overlay_font = sans:10
background = ffffff

[binds]
<Shift+less> = rotate by -90
<Shift+greater> = rotate by 90
<equal> = zoom 1
0 = zoom actual
1 = zoom actual
w = scaling full
p = prev
n = next
y = exec echo $imv_current_file

# vim: se ft=dosini:
