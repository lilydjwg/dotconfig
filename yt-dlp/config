# marked as watched about 10% at start of download
# --mark-watched
# --cookies-from-browser firefox:nightly
# unrestricted "bestvideo" will select h264 over vp9 because of its larger size
# vp9 / vp09... > hevc > avc > av1
--format=(bestvideo[height<=1080][vcodec^=vp]/bestvideo[height<=1080][vcodec^=hev]/bestvideo[height<=1080][vcodec^=avc]/bestvideo[height<=1080])+bestaudio/(best[height<=1080][vcodec^=vp]/best[height<=1080][vcodec^=hev]/best[height<=1080][vcodec^=avc]/best[height<=1080])
--socket-timeout=10
--add-metadata
--prefer-free-formats

--embed-subs
